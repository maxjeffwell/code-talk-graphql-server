import { WorkerCallback } from '../utils/types';
export declare class SVGRender {
    store: any;
    unsubscribe: any;
    viz: any;
    constructor(store: any, workerURI: string, loadWorker?: WorkerCallback);
    destroy(): void;
    _renderSvg(dot: any): void;
}
